local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 40,
  y_offset = 26,
  color = 0x00000000,
  padding_right = 6,
  padding_left = 6,
  sticky = 'on',
  topmost = 'on',
  position = 'top'
})
